# YOLOv5 🚀 by Ultralytics, AGPL-3.0 license
# Car License Plate Detection dataset https://www.kaggle.com/datasets/andrewmvd/car-plate-detection
# Example usage: python train.py --img 640 --batch 16 --epochs 3 --data ../Licence.yaml --weights yolov5s.pt

# parent
# ├── yolov5
# └── datasets
#     └── licence

path: ../datasets/licence  # dataset root dir
train: images/train        # train images (relative to 'path') 128 images
val: images/val            # val images (relative to 'path') 128 images
test: images/test          # test images (optional)

# Classes
names:
  0: licence